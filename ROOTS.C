#include<stdio.h>
#include<conio.h>
#include<math.h>

int main()
{
       int a,b,c;
       float root1,root2;

       printf("enter 'a', 'b', 'c' :\n");
       scanf("%d%d%d",&a,&b,&c);
       root1=sqrt((((b)^2)-(4(a*c)/(2*a));
       root2=-sqrt((((b)^2)-(4(a*c))/(2*a));
       printf("%f \n %f",root1,root2);
       getch();
       clrscr();
       return 0;

}